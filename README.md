# 🚀 VibeCoder: 全栈 AI 编程智能体 (Agentic Coding Engine)

基于大语言模型（LLM）构建的自动化编程与测试引擎。通过 Agentic Workflow（智能体工作流）实现了从“自然语言需求解析”到“代码生成、沙箱执行、错误捕获、自我修复”的全自动闭环。

## ✨ 核心工程特性 (Key Features)

* 🧠 **基于状态机的多轮上下文管理 (State Management)**：突破原生 LLM 无状态 API 的限制，在本地维护对话与执行日志的 Context Window，支持复杂的连招指令与逻辑修改。
* 🔄 **Agentic 自我修复闭环 (Auto-Debugging Loop)**：系统能够自动提取本地终端报错日志（Traceback），并将其作为强化上下文反哺给模型，赋予模型最多 3 次的自我纠错与迭代能力。
* 🛡️ **零信任 Docker 安全沙箱 (Zero-Trust Sandbox)**：摒弃高危的裸机执行方案，通过 Python 动态调度轻量级 Linux 容器 (`python:3.10-slim`) 运行生成代码。实施严格的内存限制 (`256M`) 与物理断网 (`--network none`)，彻底防范恶意代码与 OOM 攻击。
* 🌐 **全栈交互前端 (Gradio WebUI)**：构建了类似主流 IDE 的分屏界面，实现 Agent 思维链路、代码高亮与沙箱终端输出的实时可视化渲染。

## 🛠️ 技术栈 (Tech Stack)

* **核心大脑**: Qwen2.5-Coder-7B (基于 vLLM 部署以获得高并发推理吞吐)
* **Agent 引擎**: Python (OpenAI SDK)
* **安全容器**: Docker Desktop (WSL2 内核隔离)
* **前端展示**: Gradio (Blocks API 分屏布局)

## 🎬 项目演示 (Demo)

> 

https://github.com/user-attachments/assets/2184b0a0-c0c0-4b60-8a6c-aa095a322722



## 🚀 快速启动 (Quick Start)

### 1. 环境依赖
```bash
pip install openai gradio
请确保本地已安装并启动 Docker Desktop
docker pull python:3.10-slim


### 2. 配置模型接口
启动本地或远程的 vLLM 服务，确保兼容 OpenAI API 标准。在 `web_ui.py` 中修改 `AUTODL_API_BASE` 为你的实际地址。

### 3. 运行引擎
`bash`
python web_ui.py
`
启动后，访问 `http://127.0.0.1:7860` 即可进入 VibeCoder 控制台。
